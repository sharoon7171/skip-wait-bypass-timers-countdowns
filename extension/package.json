{
  "name": "skip-wait",
  "version": "1.4.82",
  "description": "Bypass countdown timers and skip waiting pages. Instant access on link shorteners and file hosts - no wait, one click. Works on any site with wait timers.",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite build --config vite.background.config.ts --watch\" \"vite build --config vite.popup.config.ts --watch\" \"vite build --config vite.content.config.ts --watch\"",
    "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; try { rmSync('dist',{recursive:true,force:true}); } catch {}\" && tsc -b && concurrently \"vite build --config vite.background.config.ts\" \"vite build --config vite.popup.config.ts\" \"vite build --config vite.content.config.ts\"",
    "build:zip": "bash scripts/build-zip.sh",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "concurrently": "^10.0.4",
    "tailwindcss": "^4.3.3",
    "terser": "^5.49.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
